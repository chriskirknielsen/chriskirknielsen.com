<header class="header">
    <div class="header-inner | container">
        <a href="{{ '/' | locale_url }}" class="header-logo">
            {% set homepageLabel = 'homepageLabel' | i18n %}
            {{ svg('logo', { title: homepageLabel }) }}
        </a>

        <nav class="header-nav" aria-label="{{ 'navLabel' | i18n }}">
            <ul class="header-menu menu" role="list">
                <li class="menu-item">
                    <a href="#" class="menu-link | cta" data-cta="underline">Blog</a>
                </li>
                <li class="menu-item">
                    <a href="#" class="menu-link | cta" data-cta="underline">About</a>
                </li>
                <li class="menu-item">
                    <a href="#" class="menu-link | cta" data-cta="underline">Shop</a>
                </li>
            </ul>
        </nav>
        
        {% include "components/themepicker-toggler.njk" %}
    </div>
</header>